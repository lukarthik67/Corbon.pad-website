# Copy this file to .env and fill in your own values.

# MongoDB Atlas (recommended for production) or a local mongod instance.
# Atlas gives you replica sets, automatic failover, and sharding when you need it.
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/carbonpad?retryWrites=true&w=majority

PORT=3000
